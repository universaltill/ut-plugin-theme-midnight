# Midnight Theme

Dark style theme for Universal Till POS — low-glare palette for dim
environments; keeps the default screen layout. Asset-only plugin
(`runtime: "none"`); registers one `type: "theme"` entry. After install, pick
it under Settings → Theme.
